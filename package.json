{
  "name": "genius-academy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build-vercel.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  }
}
