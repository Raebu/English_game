{
  "name": "genius-academy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build-vercel.mjs",
    "test": "node --test tests/*.test.mjs",
    "static-check": "node scripts/static-check.mjs",
    "check": "npm run static-check && npm test && npm run build"
  }
}
